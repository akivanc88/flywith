{
  "name": "flywith-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FlyWith stopover concierge — orchestrator + subagent architecture on the Anthropic SDK",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/server.ts",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.110.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
